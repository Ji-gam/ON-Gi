__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv
.*.env
.env
.idea/
.vscode
.coverage
*.coverage
.coverage.*
docker-compose.override.yml
# frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# oh-my-claudecode local state
.omc/

# chroma db data
ai_worker/chroma_data/

# macOS junk
.DS_Store

# local raw-data staging (source CSVs/DB for app/database/*.db builds)
_data/

# ai_worker/source/는 RAG 드롭 폴더다. 여기엔 RAG 재료만 둔다.
#
# DUR/e약은요 CSV 8개는 더 이상 손으로 받아 커밋하는 원본이 아니다 — MySQL이 원본이고
# (app/scripts/seed_dur.py가 이미 옮겨놨다), 이 CSV들은 uv run python -m
# ai_worker.scripts.export_source_from_mysql이 빌드 시점마다 새로 만드는 산출물이다.
# 그래서 커밋도, 서버 배포도 안 한다 — chroma_data와 같은 취급.
#
# 논문 JSON/OCR 마크다운/_tuning.yaml은 여전히 손으로 만든 원본이라 계속 커밋한다.
ai_worker/source/drugs_data.csv
ai_worker/source/dur_pwnm_taboo.csv
ai_worker/source/dur_odsn_atent.csv
ai_worker/source/dur_spcify_agrde_taboo.csv
ai_worker/source/dur_mdctn_pd_atent.csv
ai_worker/source/dur_cpcty_atent.csv
ai_worker/source/dur_efcy_dplct.csv
ai_worker/source/dur_usjnt_taboo.csv

# 제품명->성분명 조회용 사전(RAG 문서 아님, retrieve_service._load_product_ingredient_map
# 전용). 밑줄 접두어라 ingest 스캔에서는 이미 제외되지만(ai_worker/ingest/sources.py의
# _is_source_file), 다른 CSV 8개와 같은 이유로 커밋도 배포도 안 한다.
ai_worker/source/_item_ingredient_map.csv

# drug_info_sync pipeline outputs (regenerated by scripts/drug_info_sync)
app/database/drugs_full.db
scripts/drug_info_sync/progress_db.log
scripts/drug_info_sync/download_db_mode/

# FCM(Firebase Cloud Messaging) 서비스 계정 키 - 비공개키가 든 비밀 파일이라 각자 로컬에만
# 둔다. Firebase 콘솔 > 프로젝트 설정 > 서비스 계정 > "새 비공개 키 생성"으로 발급받아
# app/secrets/firebase-service-account.json에 넣으면 된다.
app/secrets/

# 개인 로컬 백업(drugs_full.db 등 app/database/*.db 원본을 담은 zip) - 커밋 대상 아님
app/database/database.zip

